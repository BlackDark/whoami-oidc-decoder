version: "2"

linters:
  default: standard
  enable:
    - errcheck
    - gosec
    - govet
    - revive
    - staticcheck
    - unused
    - ineffassign
    - misspell
    - unconvert
    - unparam

formatters:
  enable:
    - gofmt
    - goimports
